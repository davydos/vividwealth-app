# Commands to push all branches to org-origin once the repository exists:

git push org-origin main
git push org-origin develop
git push org-origin feature/auth-flow
git push org-origin feature/test-build
git push org-origin hotfix/splash-screen
# Command to push all branches to org-origin once the repository exists:

git push org-origin --all
